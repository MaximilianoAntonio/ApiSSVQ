# Azure Environment Configuration
AZURE_ENV_NAME=gestor-vehiculos-dev
AZURE_LOCATION=eastus
AZURE_SUBSCRIPTION_ID=your-subscription-id-here

# Database Configuration - Password for existing SQL Server (ssvq.database.windows.net)
DATABASE_ADMIN_PASSWORD=ssvq1!flota

# Django Configuration
DJANGO_SECRET_KEY=your-very-secure-secret-key-here-min-50-chars-long
DJANGO_DEBUG=False
CORS_ALLOWED_ORIGINS=*

# Optional: Override default resource names
# RESOURCE_GROUP_NAME=rg-gestor-vehiculos-dev

# Instructions:
# 1. Copy this file to .env
# 2. Replace AZURE_SUBSCRIPTION_ID with your Azure subscription ID
# 3. Update DATABASE_ADMIN_PASSWORD if needed (current password for ssvq database)
# 4. Generate a secure Django secret key: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# 5. Never commit the .env file to version control

# Note: This configuration uses your existing SQL Server database at ssvq.database.windows.net
# Database: ssvq
# User: ssvqdb@ssvq
